from django.apps import AppConfig


class CredentialVaultConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.credential_vault"
    verbose_name = "Credential Vault"
