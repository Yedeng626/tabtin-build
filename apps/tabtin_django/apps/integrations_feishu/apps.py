from django.apps import AppConfig


class IntegrationsFeishuConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integrations_feishu"
    label = "integrations_feishu"
    verbose_name = "飞书多维表集成"
