根据下列用户消息的主题，写一个短标题。

用户消息：
{% for msg in messages[:4] %}
{%- if msg.role == 'user' -%}
- {{ msg.content[:200] }}{% if msg.content|length > 200 %}...{% endif %}
{% endif %}
{% endfor %}

若上方用户消息为空或仅空白，直接返回：新任务
只要有可见用户正文，就概括成短标题，不要返回新任务
只返回标题文本：
