Django==4.2.7
pypinyin>=0.50,<1.0
django-ninja==1.5.3
celery==5.3.4
django-celery-beat==2.5.0
django-celery-results==2.5.1
redis==5.0.1
django-redis==5.4.0
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.1
gunicorn==21.2.0
croniter==1.4.1
openai>=1.109.1,<3.0.0
anthropic>=0.39.0,<1.0.0
# [socks] extra pulls socksio: the LLM proxy builds httpx clients with the
# default trust_env=True, so a backend running behind a SOCKS proxy
# (ALL_PROXY=socks5://...) needs the SOCKS transport to reach upstream.
httpx[http2,socks]>=0.25.2,<1.0.0
PyNaCl>=1.5.0,<2.0.0
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
python-dotenv==1.0.0
pydantic>=2.9,<3.0
PyJWT[crypto]==2.10.1
PyYAML==6.0.3

# Excel and data processing
openpyxl==3.1.2
xlrd==2.0.1
numpy==1.24.3
pandas==2.0.3
email-validator==2.3.0

# PDF generation
reportlab==4.0.7

# Database drivers
psycopg2-binary==2.9.9
sqlparse==0.5.1

# Alibaba Cloud SDKs
alibabacloud_dysmsapi20170525>=4.2.0
oss2>=2.18.0
python-magic==0.4.27

# Payment SDKs
python-alipay-sdk>=3.4.0,<4.0.0
pyOpenSSL>=24.0.0,<26.0.0
pycryptodomex>=3.20.0,<4.0.0
wechatpayv3>=1.3.11,<2.0.0
qrcode[pil]>=8.2,<9.0

# LangChain Core（BaseTool 依赖） + LiteLLM（统一 LLM 调用）
langchain-core>=1.2.0
litellm>=1.56.0,<1.80.0

# Vector Database (用于 RAG)
pgvector==0.2.4

# Speech Services (ASR WebSocket streaming)
aiohttp>=3.9.0

# Headless Browser (tabdesign/tabslide/tabvideo/TabDoc PDF 导出渲染用；部署镜像内安装 Chromium)
playwright>=1.40.0

# PDF parsing (DocParse)
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# PPTX parsing (TabSlide + DocParse)
python-pptx>=0.6.23

# DOCX reading & writing (DocParse import + TabDoc export)
python-docx>=1.1.0

# SSH remote execution
paramiko>=4.0.0

# Security / HTML sanitization
nh3>=0.2.14
bleach>=6.0.0,<7.0
tinycss2>=1.2.0  # Required by bleach CSSSanitizer for preserving inline CSS styles
defusedxml>=0.7.1

# AWS
boto3>=1.34.0

# Google API (Gmail)
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.90.0

# Monitoring
prometheus-client>=0.20.0
# Sentry 错误监控（issue #3299）：env 注入 SENTRY_DSN 时启用；
# 未配置 DSN / 未安装时 tabtin/sentry.py 静默降级，零开销
sentry-sdk>=2.0.0,<3.0.0

# FTS 统一搜索引擎（PRD 2026-04-16 / ADR-14）
# elasticsearch-py 8.13.x ~ 8.14.x：对齐当前支持的 ES 服务端版本。
# **严格不跨 8.15**：8.15+ 客户端引入 retriever v2 新参数，发给 8.13/8.14
# 服务端会返回 HTTP 400 `unknown parameter`。服务端升级到 8.15+ 后，
# 再解除此上限。
elasticsearch>=8.13.0,<8.15.0
pybreaker>=1.2.0,<2.0.0

# OpenTelemetry tracing（R5-03 修复）：
# - api/sdk：apps/fts/otel_trace.py + apps/services/agent_engine/middleware/otel_trace.py
#   通过 try-import 兼容；安装后 span 真起来
# - exporter-otlp-proto-grpc：将 span 发到 OTLP 后端
#   （阿里云 ARMS / Jaeger / Tempo 都支持 OTLP/gRPC 协议）
# 启用条件：env 注入 `OTEL_EXPORTER_OTLP_ENDPOINT=https://...`
# 不注入时 setup 不调 register → 默认 NoOpTracerProvider，零开销
opentelemetry-api>=1.20.0,<2.0.0
opentelemetry-sdk>=1.20.0,<2.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0,<2.0.0

# ─────── 获客分析：离线 IP → 地域（#4002 地域分布）───────
# py-ip2region：官方 ip2region python binding，配套离线库
# apps/analytics/data/ip2region_v4.xdb，把访客 IP 现算成国家/省份，
# 只落地域、不存原始 IP（隐私）。纯离线查询，无外呼。
py-ip2region>=3.0.4,<4.0.0

# ─────── 测试依赖（本地与公开 CI 跑 pytest） ───────
# pytest-django: 提供 @pytest.mark.django_db / django_db_setup fixture
pytest>=7.4,<8.0
pytest-django>=4.7,<5.0
