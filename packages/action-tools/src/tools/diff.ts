/**
 * @deprecated compute_diff 已永久移除。
 * 保留此文件仅为避免外部导入报错，所有导出均为空 stub。
 */

import type { AgentTool } from '../types'

export const computeDiffTool = undefined as unknown as AgentTool
export const diffTools: AgentTool[] = []
