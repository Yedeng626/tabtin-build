"""tabtin-demo-app CLI — GitHub Issue 管理演示。

使用方法：
    python -m cli issue create --title "Bug fix" --repo "owner/repo"
    python -m cli issue list --repo "owner/repo"
    python -m cli issue get --repo "owner/repo" --number 1
    python -m cli issue close --repo "owner/repo" --number 1
    python -m cli auth login
    python -m cli auth status
"""
