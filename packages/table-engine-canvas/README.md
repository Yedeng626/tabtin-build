# @tabtin/table-engine-canvas

Canvas-based renderer engine for TabTin table.

## Scope
- Provide a migration-safe experimental canvas grid
- Keep the same contract as other engines so host can switch by config
